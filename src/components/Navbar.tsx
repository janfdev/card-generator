const Navbar = () => {
  return (
    <nav className="sticky top-0 flex items-center justify-start rounded-full mt-3 bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 text-white py-2 px-6 w-full">
      <h1 className="text-2xl font-bold">Card Generator</h1>
    </nav>
  );
};

export default Navbar;
